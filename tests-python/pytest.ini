[pytest]
# 测试用例文件命名规则
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 输出：-v 显示每个用例名，-ra 显示失败摘要
addopts = -v -ra --alluredir=../target/allure-results-python --clean-alluredir

# 单个用例超时保护（需要 pytest-timeout，未装则忽略）
# timeout = 60
