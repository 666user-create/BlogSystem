# 07 接口测试与抓包分析说明

| 项目 | 内容 |
|---|---|
| 文档编号 | BS-TEST-DOC-07 |
| 被测系统 | BlogSystem 博客系统 v0.0.1（11 个接口） |
| 技术栈 | Java + RestAssured + JUnit5（30 条）／ Python + pytest + requests + Allure（40 条）／ 自建抓包代理（3 条） |
| 编写日期 | 2026-08 |

## 1. 双技术栈说明

同一批接口用例用两套技术栈实现，覆盖企业常见组合：

| 技术栈 | 位置 | 用例数 | 定位 |
|---|---|---|---|
| **Java + RestAssured** | `src/test/java/.../api/` | 30 | 与工程同栈，随 `mvn test` 执行；可 @MockBean 替换 Feign 依赖 |
| **Python + pytest** | `tests-python/` | 40 | 独立于工程语言，可直接对接测试/预发环境；Allure 报告 |

两套用例覆盖同一批接口，互为交叉验证（例如参数校验 400、越权拒绝、401 拦截在两套中都有断言）。

## 2. Java 侧：RestAssured（30 条）

| 测试类 | 用例数 | 覆盖 |
|---|---|---|
| UserApiTest | 10 | 注册成功/重复用户名/参数校验、登录成功/不存在/密码错误、用户信息、作者信息 |
| BlogApiTest | 11 | 列表、详情（含 `blogId=0` → 400）、新增（空标题/超长标题）、更新（作者/越权）、删除（作者/越权） |
| AdminApiTest | 5 | 管理员列表、上下架切换、非管理员拒绝、不存在博客 |
| AuthApiTest | 4 | 无 token / 空 token / 篡改 token → 401，登录注册免鉴权 |

执行：`mvn test -Dtest=*ApiTest`（`@SpringBootTest` 随机端口 + 独立 test profile）

## 3. Python 侧：pytest + requests（40 条）

### 3.1 目录结构

```
tests-python/
├── requirements.txt          # pytest / requests / allure-pytest
├── pytest.ini                # 用例发现规则 + Allure 结果目录
├── conftest.py               # fixture：健康检查、随机用户、token、登录态、越权账号
├── proxy_server.py           # 抓包代理（Fiddler 的可执行替代）
├── test_user_api.py          # 用户模块（12 条）
├── test_blog_api.py          # 博客模块（16 条）
├── test_admin_api.py         # 管理端（6 条）
└── test_security_capture.py  # 抓包与安全检查（3 条）
```

### 3.2 fixture 设计（避免用例间耦合）

| fixture | 作用 |
|---|---|
| `base_url`（session） | 读取 `BASE_URL` 环境变量，会话开始时做健康检查，服务未启动直接给出启动指引 |
| `new_user` | 注册随机用户名（前缀 + 时间戳），保证用例可重复执行 |
| `token` / `auth` | 新用户登录后的 token 与请求头 |
| `other_user` | **另一个独立用户**，专用于"非作者越权"用例（避免复用 auth 导致自己改自己） |
| `admin_headers` | 管理员登录态（admin/admin123） |

> 踩坑记录：最初越权用例直接复用了 `new_user`/`token`，导致"用户 B"其实就是作者本人，断言失败；抽出独立的 `other_user` fixture 后修复。

### 3.3 用例分布（40 条）

| 模块 | 条数 | 主要断言点 |
|---|---|---|
| 用户模块 | 12 | 注册成功/重名/参数校验 400；登录成功返回 token/用户不存在/密码错误；用户信息与博客数；作者信息 |
| 博客模块 | 16 | 列表数组与字段完整性；详情正常/不存在/非法 blogId 400；新增成功且出现在列表/空标题/超长标题/未登录 401；更新成功/越权；删除成功（逻辑删除后查不到）/越权/不存在 |
| 管理端 | 6 | 管理员列表；非管理员"无管理员权限"；未登录 401；上下架切换（含下架后不在普通列表）；不存在博客 |
| 安全与抓包 | 3 | 抓包验证登录请求明文传输、响应头无敏感信息、SQL 注入尝试不产生 500 |

### 3.4 执行与结果

```bash
# 前置：启动被测应用
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 执行（在 tests-python 目录）
python -m pytest                 # 全量
python -m pytest -k login        # 按关键字
```

| 指标 | 结果 |
|---|---|
| 用例总数 | 40 |
| 通过 | **40（100%）** |
| 失败 | 0 |
| 执行耗时 | 约 2.4 秒（接口用例 1.78s + 抓包用例 0.60s） |

## 4. 抓包分析（Fiddler 场景的自动化实现）

### 4.1 为什么不用 Fiddler 直接跑

Fiddler 是 GUI 工具，抓包过程依赖人工操作，无法纳入自动化回归。

本工程用 **自建 HTTP 抓包代理**（`proxy_server.py`）实现同样的验证目标：

```
pytest 用例  →  本地抓包代理(127.0.0.1:8899)  →  被测应用(127.0.0.1:8080)
                        ↓
              记录请求行/请求头/请求体 → 落盘 JSON 证据
```

对应用例：
- **TC-SEC-05**：通过代理登录，断言抓包记录包含该请求，并输出请求明细；
- **TC-SEC-06**：断言响应头不含 `password/secret/token=/authorization` 等敏感字段，响应体不回显密码；
- **TC-SEC-02**：用户名传入 `' OR '1'='1`，断言不产生 500、不泄露 SQL 异常。

证据文件：`docs/test-evidence/capture/login-capture.json`

### 4.2 抓包结论（重要发现）

| 结论 | 说明 | 风险等级 |
|---|---|---|
| 登录请求**明文传输密码** | 抓包可见请求体 `{"userName":"...","password":"123456"}`；当前部署为 HTTP（8080），未启用 HTTPS | **中**（生产环境必须启用 HTTPS，否则明文密码可被链路窃听） |
| 响应头无敏感信息泄露 | 未发现密码/密钥类响应头 | 通过 |
| 密码不回显 | 登录响应仅返回 userId/token/userName | 通过 |
| SQL 注入不生效 | 注入串走正常业务判断（参数校验拦截或"用户不存在"），无 500、无堆栈泄露 | 通过 |

> 该结论与缺陷清单 BUG-08（登录无频率限制）同属"安全加固"建议项，已记录在 `docs/缺陷清单.md`。

## 5. Allure 报告

```bash
# 生成结果（pytest 已配置 --alluredir）
python -m pytest

# 渲染 HTML 报告（需 allure 命令行）
allure generate ../target/allure-results-python -o ../reports/allure-python --clean
allure open ../reports/allure-python
```

Python 侧用例已用 `@allure.feature / @allure.story / @allure.title / @allure.severity / @allure.step` 标注，报告按"模块 → 用例"分组展示，并可附抓包明细等附件。
