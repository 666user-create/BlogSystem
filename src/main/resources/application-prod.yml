# 生产环境: 覆盖数据库密码和日志配置
spring:
  datasource:
    password: 2746993199@qQ

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

logging:
  file:
    # 日志统一收进 logs/ 目录
    name: logs/spring-blog-prod.log
