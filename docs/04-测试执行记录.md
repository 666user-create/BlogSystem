# 04 测试执行记录

| 项目 | 内容 |
|---|---|
| 文档编号 | BS-TEST-DOC-04 |
| 被测系统 | BlogSystem 博客系统 v0.0.1 |
| 编写人 | 测试开发工程师 |
| 执行周期 | 2026-08 |
| 执行轮次 | 首轮全量 + 缺陷修复回归 + 最终全量回归 |

## 1. 执行环境

| 项 | 配置 |
|---|---|
| 操作系统 | Windows 10/11 |
| JDK / 构建 | JDK 23.0.2，Maven 3.9.16（编译目标 Java 17） |
| 数据库 | MySQL 8（java_blog_spring，utf8mb4） |
| 应用 | Spring Boot 3.3.5，端口 8080 |
| 浏览器 | Chrome 最新稳定版（Selenium WebDriver） |
| 工具 | JUnit5（单元）、Postman（接口）、Selenium（UI） |

## 2. 执行概况

| 类别 | 用例数 | 首轮通过 | 首轮失败 | 回归后通过 | 回归后失败 |
|---|---|---|---|---|---|
| 接口用例（RestAssured / Postman） | 84 | 75 | 9 | 79 | 5 |
| UI 用例（Selenium） | 6 | 5 | 1 | 6 | 0 |
| 单元测试（JUnit5 + Mockito） | 34 | 34 | 0 | 34 | 0 |
| **合计** | **124** | **114** | **10** | **119** | **5** |

> 接口用例 = 90 条用例中除 6 条 UI 用例外的 84 条。单元测试为工具类 + 服务层测试，单列统计。
> **最终通过率（接口+UI）：85/90 = 94.4%**；含单元测试：119/124 = 96.0%。

## 3. 自动化测试执行结果（当前基线，全部通过）

### 3.1 单元测试（JUnit5 + Mockito）

执行命令：`mvn test -Dtest=...*Test` 或全量 `mvn test`

| 测试类 | 用例数 | 通过 | 失败 | 说明 |
|---|---|---|---|---|
| JwtUtilsTest | 6 | 6 | 0 | JWT 签发/解析、篡改 token、过期 token、空值、取用户名/ID |
| SecurityUtilTest | 5 | 5 | 0 | 密码加盐哈希往返、错误密码、null 输入、盐随机性、长度 64 |
| UserServiceImplTest | 9 | 9 | 0 | 登录全分支、注册（密码一致性/重名/加密存储）、用户信息、作者查询 |
| BlogServiceImplTest | 14 | 14 | 0 | 增删改查全分支、作者权限、逻辑删除、上下架切换、统计 |
| **合计** | **34** | **34** | **0** | 通过率 100% |

### 3.2 接口自动化测试（RestAssured + @SpringBootTest）

执行命令：`mvn test -Dtest=*ApiTest`（需本机 MySQL 启动；使用独立 test profile，见 `src/test/resources/application-test.yml`）

| 测试类 | 用例数 | 通过 | 失败 | 说明 |
|---|---|---|---|---|
| UserApiTest | 10 | 10 | 0 | 注册/登录/用户信息（TC-REG/LOG/UINF） |
| BlogApiTest | 11 | 11 | 0 | 列表/详情/新增/更新/删除（TC-LIST/DET/ADD/UPD/DEL） |
| AdminApiTest | 5 | 5 | 0 | 管理端列表/上下架/权限（TC-ADM） |
| AuthApiTest | 4 | 4 | 0 | 无/空/篡改 token（TC-AUTH） |
| **合计** | **30** | **30** | **0** | 通过率 100% |

> 执行过程中接口自动化发现并驱动修复了 BUG-12（参数校验异常未返回 400），见缺陷清单。

## 4. 首轮执行失败用例明细（10 条）

| 用例编号 | 模块 | 实际结果 | 关联缺陷 | 缺陷严重级别 | 处理状态 |
|---|---|---|---|---|---|
| TC-AUTH-06 | 鉴权 | 携带登录 token 调用接口返回 401，token 未生效 | BUG-01（token 头名不一致） | 高 | 已修复 |
| TC-ADD-07 | 新增 | 未登录仍可调用 addBlog（未被拦截） | BUG-02（写博客未鉴权） | 高 | 已修复 |
| TC-UI-02 | UI | 未登录可进入写博客页并尝试发表 | BUG-02 | 高 | 已修复 |
| TC-SEC-03 | 安全 | 登录失败日志包含明文密码与密码哈希 | BUG-04（密码写入日志） | 严重 | 已修复 |
| TC-DET-04 | 详情 | `blogId=0` 期望 400，实际返回 200（参数校验异常未统一处理） | BUG-12（ConstraintViolationException 未处理） | 中 | 已修复 |
| TC-LOG-03 | 登录 | 密码错误响应仅有 code=-1+文案，无细分业务错误码 | BUG-03（错误码未落地） | 中 | 部分修复 |
| TC-LIST-05 | 列表 | 1000 条数据全量返回，响应时间随数据量线性增长 | BUG-06（无分页） | 中 | 保留 |
| TC-LIST-06 | 列表 | 未登录访问列表返回 401，游客无法浏览 | BUG-07（游客 401） | 中 | 保留 |
| TC-LOG-09 | 登录 | 连续 10 次错误密码无任何限流 | BUG-08（无限流） | 中 | 保留 |
| TC-SEC-01 | 安全 | 博客内容注入 `<script>` 原样存储并渲染 | BUG-11（XSS 未过滤） | 中 | 保留 |

## 5. 缺陷发现途径统计

| 发现途径 | 缺陷编号 | 数量 |
|---|---|---|
| 开发自测 | BUG-01、BUG-02、BUG-03 | 3 |
| 接口测试（Postman） | BUG-04、BUG-06、BUG-07、BUG-08、BUG-11 | 5 |
| 代码审查 | BUG-05（密钥硬编码）、BUG-09（越权查询）、BUG-10（权限模型） | 3 |
| **合计** | - | **11** |

## 6. 回归执行记录

### 6.1 缺陷修复回归（第一轮）

| 修复提交 | 修复缺陷 | 回归用例 | 结果 |
|---|---|---|---|
| `f3adba4` 登录日志脱敏 | BUG-04 | TC-SEC-03 | ✅ 通过：错误密码登录后日志仅含用户名 |
| `50fab90` JWT 密钥配置化 | BUG-05 | TC-AUTH-01/02/03、TC-LOG-01 | ✅ 通过：默认配置下鉴权链路正常；环境变量覆盖密钥后旧 token 失效（符合预期） |
| 前端 token 双写 + 后端双头兼容 | BUG-01 | TC-AUTH-06、TC-LOG-01、TC-ADD-01 | ✅ 通过：携带 token 调用受保护接口正常 |
| 后端拦截器 + 前端 401 兜底 | BUG-02 | TC-ADD-07、TC-UI-02 | ✅ 通过：未登录发表被 401 拦截并跳转登录页 |
| `59b95e3` 参数校验异常统一 400 | BUG-12 | TC-DET-04 | ✅ 通过：`blogId=0` 返回 400 |

### 6.2 最终全量回归

- 执行范围：全部 90 条用例 + 11 条单元测试
- 结果：接口/UI 通过 85，失败 5（全部为已评估遗留缺陷，见下表）
- 结论：达到准出标准（P0/P1 用例全部通过，遗留项均有缺陷记录与风险说明）

### 6.3 遗留失败用例（回归后仍失败，5 条）

| 用例 | 关联缺陷 | 遗留原因与风险评估 |
|---|---|---|
| TC-LOG-03 | BUG-03 | 业务错误码枚举已定义未接入，前端靠文案判断；影响体验，不影响功能正确性 |
| TC-LIST-05 | BUG-06 | 无分页；当前数据量小无实际影响，数据量增长后需优化 |
| TC-LIST-06 | BUG-07 | 游客无法浏览列表；取决于产品定位（是否允许游客访问），需产品决策 |
| TC-LOG-09 | BUG-08 | 登录无频率限制；存在暴力破解风险，个人场景风险可控 |
| TC-SEC-01 | BUG-11 | 博客内容无 XSS 过滤；单用户使用场景风险可控，多人场景需修复 |

## 7. 测试数据使用记录

| 数据 | 说明 | 使用模块 |
|---|---|---|
| admin / admin123 | 应用启动自动创建 | 管理端、权限矩阵 |
| userA / 123456（含博客若干） | 造数/注册 | 正常流、作者权限 |
| userB / 123456 | 造数/注册 | 非作者越权用例 |
| 上架/下架/已删除/超长标题博客 | SQL 造数 | 列表过滤、边界值 |
| 有效/篡改/过期/空 token | 工具构造 | 鉴权用例 |

## 8. 结论

1. 首轮共发现 10 个失败用例，对应 9 个缺陷，其中严重 1 个（BUG-04）、高 2 个（BUG-01、BUG-02）及 BUG-12 已全部修复并通过回归；
2. 遗留 5 条失败用例均有明确缺陷记录、风险说明与处理建议，无 P0/P1 功能性阻塞；
3. 自动化测试基线 65/65 通过（单元 34 + 接口 30 + 启动冒烟 1），自动化回归能力已建立（后续可接入 CI 持续执行）。
