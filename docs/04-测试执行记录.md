# 04 测试执行记录

| 项目 | 内容 |
|---|---|
| 文档编号 | BS-TEST-DOC-04 |
| 被测系统 | BlogSystem 博客系统 v0.0.1 |
| 编写人 | 测试开发工程师 |
| 执行周期 | 2026-08 |
| 执行轮次 | 首轮全量 + 缺陷修复回归 + 最终全量回归 |

## 1. 执行环境

| 项 | 配置 |
|---|---|
| 操作系统 | Windows 10/11 |
| JDK / 构建 | JDK 23.0.2，Maven 3.9.16（编译目标 Java 17） |
| 数据库 | MySQL 8（java_blog_spring，utf8mb4） |
| 应用 | Spring Boot 3.3.5，端口 8080 |
| 浏览器 | Chrome 最新稳定版（Selenium WebDriver） |
| 工具 | Postman（接口调试）、Selenium（UI 自动化）、pytest（接口自动化）、抓包代理（Fiddler 场景）、JMeter（性能）、Allure（报告） |

## 2. 执行概况

| 类别 | 用例数 | 首轮通过 | 首轮失败 | 回归后通过 | 回归后失败 |
|---|---|---|---|---|---|
| 接口用例（pytest / Postman） | 87 | 78 | 9 | 83 | 4 |
| UI 用例（Selenium） | 6 | 5 | 1 | 6 | 0 |
| **合计** | **93** | **83** | **10** | **89** | **4** |

> 接口用例 = 93 条设计用例中除 6 条 UI 用例外的 87 条（其中 TC-LIST-09/10/11 为 BUG-06 修复后补充的分页用例，首轮即通过）。
> **最终通过率（接口+UI）：89/93 = 95.7%**。

## 3. 自动化测试执行结果（当前基线，全部通过）

### 3.1 UI 自动化测试（Selenium WebDriver + JUnit5）

执行命令：`mvn test -Dtest=BlogUiTest`（Headless Chrome，测试自启动应用随机端口）

| 测试类 | 用例数 | 通过 | 失败 | 说明 |
|---|---|---|---|---|
| BlogUiTest | 33 | 33 | 0 | 登录 6 + 注册 6 + 列表发布 4 + 详情编辑删除 4 + 登录态权限 3 + 数据展示正确性 3 + 作者权限与归属 4 + 列表分页 2 + 样式生效 1 |
| **合计** | **33** | **33** | **0** | 通过率 100%，耗时 91.7 秒，产出 34 张截图 + 弹框文本证据 |

> 详见 `docs/06-UI自动化测试说明.md`；截图与弹框文本证据在 `docs/test-evidence/ui/`（跑 `mvn test -Dtest=BlogUiTest` 本地生成，不入库）。
> 注：数据库测试数据累积到 76 篇上架博客时，本套 UI 测试曾劣化至 325 秒（6.5 倍），这是 **BUG-06（列表无分页）** 的页面级影响实证；分页修复后单页固定 10 条，上一次执行（32 条用例、数据库 126 篇博客）只用 **80.4 秒**，不再随数据量增长。
> 补充：使用者反馈"列表页 UI 没了"（BUG-18，浏览器缓存旧 CSS 导致卡片样式整体失效）暴露出"只断言元素存在"的盲区，因此新增 UI-33 断言计算样式——**该用例在缺少 `list.css` 时确实会失败**（已反向验证）。

### 3.2 接口自动化测试（Python + pytest + requests）

执行命令：`python -m pytest`（需先启动应用，见 `tests-python/conftest.py` 的健康检查）

| 测试文件 | 用例数 | 通过 | 失败 | 说明 |
|---|---|---|---|---|
| test_user_api.py | 11 | 11 | 0 | 注册/登录/用户信息/作者信息 |
| test_blog_api.py | 19 | 19 | 0 | 列表/分页(5)/详情/新增/更新/删除（含越权） |
| test_admin_api.py | 6 | 6 | 0 | 管理端列表/上下架/权限 |
| test_auth_api.py | 6 | 6 | 0 | 有效/篡改/过期/空 token 鉴权 |
| test_security_capture.py | 3 | 3 | 0 | 抓包明文分析、响应头检查、SQL 注入 |
| **合计** | **45** | **45** | **0** | 通过率 100%，耗时约 3.1 秒 |

> 详见 `docs/07-接口测试与抓包分析说明.md`；抓包证据在 `docs/test-evidence/capture/`（跑抓包用例时本地生成，不入库）。

### 3.3 抓包分析（Fiddler 场景的自动化替代）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 登录请求传输方式 | HTTP 明文（密码可见），生产需启用 HTTPS | `docs/test-evidence/capture/login-capture.json`（本地生成） |
| 响应头敏感信息 | 未发现 password/secret/token 类字段 | 用例 TC-SEC-06 通过 |
| 密码回显 | 响应不含密码 | 用例 TC-SEC-06 通过 |
| SQL 注入 | 无 500、无堆栈泄露 | 用例 TC-SEC-02 通过 |

## 4. 首轮执行失败用例明细（10 条）

| 用例编号 | 模块 | 实际结果 | 关联缺陷 | 缺陷严重级别 | 处理状态 |
|---|---|---|---|---|---|
| TC-AUTH-06 | 鉴权 | 携带登录 token 调用接口返回 401，token 未生效 | BUG-01（token 头名不一致） | 高 | 已修复 |
| TC-ADD-07 | 新增 | 未登录仍可调用 addBlog（未被拦截） | BUG-02（写博客未鉴权） | 高 | 已修复 |
| TC-UI-02 | UI | 未登录可进入写博客页并尝试发表 | BUG-02 | 高 | 已修复 |
| TC-SEC-03 | 安全 | 登录失败日志包含明文密码与密码哈希 | BUG-04（密码写入日志） | 严重 | 已修复 |
| TC-DET-04 | 详情 | `blogId=0` 期望 400，实际返回 200（参数校验异常未统一处理） | BUG-12（ConstraintViolationException 未处理） | 中 | 已修复 |
| TC-LOG-03 | 登录 | 密码错误响应仅有 code=-1+文案，无细分业务错误码 | BUG-03（错误码未落地） | 中 | 部分修复 |
| TC-LIST-05 | 列表 | 1000 条数据全量返回，响应时间随数据量线性增长 | BUG-06（无分页） | 中 | 已修复 |
| TC-LIST-06 | 列表 | 未登录访问列表返回 401，游客无法浏览 | BUG-07（游客 401） | 中 | 保留 |
| TC-LOG-09 | 登录 | 连续 10 次错误密码无任何限流 | BUG-08（无限流） | 中 | 保留 |
| TC-SEC-01 | 安全 | 博客内容注入 `<script>` 原样存储并渲染 | BUG-11（XSS 未过滤） | 中 | 保留 |

## 5. 缺陷发现途径统计

| 发现途径 | 缺陷编号 | 数量 |
|---|---|---|
| 开发自测 | BUG-01、BUG-02、BUG-03 | 3 |
| 代码审查 | BUG-04（日志明文）、BUG-05（密钥硬编码）、BUG-06（无分页）、BUG-08（无限流）、BUG-10（权限模型）、BUG-15（Markdown 未渲染）、BUG-16（表头字段不符） | 7 |
| 接口 / 接口自动化测试 | BUG-07（游客 401）、BUG-09（越权查询）、BUG-12（参数校验未统一 400） | 3 |
| 安全测试（抓包 / 注入 / XSS） | BUG-11（XSS 未过滤） | 1 |
| 手工探索 / 需求走查 | BUG-13（404 图片）、BUG-14（编辑不回显）、BUG-17（按钮权限未控制） | 3 |
| 使用者反馈 | BUG-18（新旧静态资源版本错配导致样式整体失效） | 1 |
| **合计** | - | **18** |

> 后续 7 条缺陷（BUG-12 ~ BUG-18）是补充"数据正确性 / 页面展示 / 分页 / 样式生效"用例后新发现的，说明"只看操作是否成功"的用例会漏掉"操作成功但结果是错的/页面已经坏了"这类问题。

## 6. 回归执行记录

### 6.1 缺陷修复回归（第一轮）

| 修复提交 | 修复缺陷 | 回归用例 | 结果 |
|---|---|---|---|
| `f3adba4` 登录日志脱敏 | BUG-04 | TC-SEC-03 | ✅ 通过：错误密码登录后日志仅含用户名 |
| `50fab90` JWT 密钥配置化 | BUG-05 | TC-AUTH-01/02/03、TC-LOG-01 | ✅ 通过：默认配置下鉴权链路正常；环境变量覆盖密钥后旧 token 失效（符合预期） |
| 前端 token 双写 + 后端双头兼容 | BUG-01 | TC-AUTH-06、TC-LOG-01、TC-ADD-01 | ✅ 通过：携带 token 调用受保护接口正常 |
| 后端拦截器 + 前端 401 兜底 | BUG-02 | TC-ADD-07、TC-UI-02 | ✅ 通过：未登录发表被 401 拦截并跳转登录页 |
| `59b95e3` 参数校验异常统一 400 | BUG-12 | TC-DET-04 | ✅ 通过：`blogId=0` 返回 400 |

### 6.2 最终全量回归

- 执行范围：全部 93 条用例 + 78 条自动化用例（UI 33 + 接口 45）
- 结果：接口/UI 通过 89，失败 4（全部为已评估遗留缺陷，见下表）
- 结论：达到准出标准（P0/P1 用例全部通过，遗留项均有缺陷记录与风险说明）

### 6.3 遗留失败用例（回归后仍失败，4 条）

| 用例 | 关联缺陷 | 遗留原因与风险评估 |
|---|---|---|
| TC-LOG-03 | BUG-03 | 业务错误码枚举已定义未接入，前端靠文案判断；影响体验，不影响功能正确性 |
| TC-LIST-06 | BUG-07 | 游客无法浏览列表；取决于产品定位（是否允许游客访问），需产品决策 |
| TC-LOG-09 | BUG-08 | 登录无频率限制；存在暴力破解风险，个人场景风险可控 |
| TC-SEC-01 | BUG-11 | 博客内容无 XSS 过滤；单用户使用场景风险可控，多人场景需修复 |

## 7. 测试数据使用记录

| 数据 | 说明 | 使用模块 |
|---|---|---|
| admin / admin123 | 应用启动自动创建 | 管理端、权限矩阵 |
| userA / 123456（含博客若干） | 造数/注册 | 正常流、作者权限 |
| userB / 123456 | 造数/注册 | 非作者越权用例 |
| 上架/下架/已删除/超长标题博客 | SQL 造数 | 列表过滤、边界值 |
| 有效/篡改/过期/空 token | 工具构造 | 鉴权用例 |

## 8. 结论

1. 首轮共发现 10 个失败用例，对应 9 个缺陷，其中严重 1 个（BUG-04）、高 2 个（BUG-01、BUG-02）及 BUG-06、BUG-12 已全部修复并通过回归；
2. 遗留 4 条失败用例均有明确缺陷记录、风险说明与处理建议，无 P0/P1 功能性阻塞；
3. 自动化测试 **78 条全部通过**（UI 自动化 33 + 接口自动化 45），自动化回归能力已建立（后续可接入 CI 持续执行）；补充用例后累计发现 18 个缺陷，已修复 12 个、部分修复 1 个、保留 5 个。
